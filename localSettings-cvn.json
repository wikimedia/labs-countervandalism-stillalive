{
	"parameters": {
		"cvn": "/data/project/cvn",
		"cvn-bin": "{ cvn }/app",
		"cvn-log": "{ cvn }/log",
		"cvn-src": "{ cvn }/externals"
	},
	"cwd": "/tmp",
	"tasks": [
		{
			"cmd": "{cvn-bin}/cvnclerkbot.sh > {cvn-log}/cvnclerkbot.log 2>&1",
			"match": "python {cvn-src}/CVNClerkBot/cvnclerkbot.py",
			"pool": "cvn-app5"
		}
	],
	"templates": {
		"CVNBot": {
			"cwd": "{cvn-bin}/{name}",
			"cmd": "mono {cvn-bin}/{name}/CVNBot.exe > {cvn-log}/{name}.log 2>&1",
			"match": "mono {cvn-bin}/{name}/CVNBot.exe"
		}
	},
	"template-tasks": {
		"CVNBot": [
			{ "name": "CVNBot2", "pool": "cvn-app4" },
			{ "name": "CVNBot3", "pool": "cvn-app4" },
			{ "name": "CVNBot4", "pool": "cvn-app4" },
			{ "name": "CVNBot12", "pool": "cvn-app4" },
			{ "name": "CVNBot13", "pool": "cvn-app5" },
			{ "name": "CVNBot14", "pool": "cvn-app5" }
		]
	}
}
